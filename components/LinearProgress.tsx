import LinearProgressMaterial from "@material-ui/core/LinearProgress";
import { styled } from "@material-ui/core";

export default styled(LinearProgressMaterial)({
  height: 6,
  borderRadius: 9999,
});
